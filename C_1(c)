#include <iostream>

using namespace std;

void delete_char(char a[], int n) {
	int size = 0;
	char* i = a;
	while (*(i + size) != '\0') {
		size++;
	}

	while(size < n){
		*(a + size ) = '2';
		size++;
	}
	
	*(a + size) = '\0';

}

